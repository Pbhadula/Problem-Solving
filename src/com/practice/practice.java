package com.practice;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class practice {

    public static void main(String[] args) {

//        Integer a[] = {1,2,3,4,5,6,7,8,9};
//        int result = Arrays.stream(a).filter(n -> n%2==0).reduce(1, (c,b) -> c+b);
//        System.out.println(result);
//
//        String str[] = {"apple","mango","rat","cat","lion"};
//
//        String result = Arrays.stream(str).filter(n -> n.length()<4).collect(Collectors.joining(","));
//
//        System.out.println(result);

//        List<String> words = Arrays.asList("apple","mango","rat","cat","lion");
//        List<String> result = words.stream().filter(s -> s.length()>3).map(s -> s.toUpperCase()).collect(Collectors.toList());
//        System.out.println(result);

        List<Integer> num = Arrays.asList(1,2,3,4,5,6,7,8,9);

        


    }
}
